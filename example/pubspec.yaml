name: example
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  benchmark_harness: any
  objectid:
    path: ../

dev_dependencies:
  pedantic: ^1.9.0
